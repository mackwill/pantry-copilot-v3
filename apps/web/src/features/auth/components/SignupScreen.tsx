import { Wordmark } from '@pantry/design-system/web';

/** Placeholder for the Task 9 SSR spike — the composed screen lands in Task 11. */
export function SignupScreen() {
  return <Wordmark size={26} />;
}
